import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonalInformation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Personal information',
  },
});
